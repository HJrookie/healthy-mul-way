[build]
  command = "npm run build"
  publish = "dist"

# SPA 回退：任何未直接命中的路径（如直接刷新、旧链接）都交给 index.html，
# 由前端路由接管。已预渲染的文章/首页为静态文件，Netlify 会优先返回它们。
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
